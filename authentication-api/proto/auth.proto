syntax = "proto3";

package auth;

option go_package = "/";

service AuthService {
  rpc IsAuthenticated(AuthRequest) returns (AuthResponse);
}

message AuthRequest {
  string token = 1;
}

message AuthResponse {
  bool isAuthenticated = 1;
}

